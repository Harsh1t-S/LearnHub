PORT=5000
# Either set DATABASE_URL (hosted MySQL, e.g. Railway) ...
DATABASE_URL=mysql://user:password@host:port/database
# ...or set these discrete vars instead (used for local MySQL):
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=learnhub
JWT_SECRET=change_this_to_a_long_random_secret_string
ADMIN_INVITE_CODE=letmein-admin
CLIENT_URL=http://localhost:5173
